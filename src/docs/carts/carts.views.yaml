paths:
  /views/carts/:
    get:
      summary: Obtiene la vista del carrito asignado al usuario
      tags:
        - Carts
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Operación exitosa. Se retorna la vista del carrito con o sin productos.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Cart"
        "404":
          description: Carrito no encontrado.
        "500":
          description: Error al cargar el carrito.
